import ScreenHome from '../components/screens/home';

/**
 * Pantalla principal de la aplicación (Home).
 * 
 * En el futuro, este archivo servirá como orquestador para cargar el dashboard
 * seleccionado por el usuario o redirigir según las preferencias guardadas.
 * 
 * @returns El componente del dashboard principal (ScreenHome).
 */
export default function Page() {
  return <ScreenHome />;
}
